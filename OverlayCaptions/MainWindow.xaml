<Window x:Class="OverlayCaptions.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Caption Overlay" 
        Height="150" Width="800"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Grid Background="Transparent">
        <Border Background="#50000000" CornerRadius="5" Padding="10" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Name="CaptionText"
                       Text="Captions will appear here..."
                       FontFamily="pack://application:,,,/Fonts/#My Font"
                       FontSize="24"
                       Foreground="White"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center" />
        </Border>
    </Grid>
</Window>
